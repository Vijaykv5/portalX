[Unit]
Description=Portalx Tunnel Server
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/portalx
ExecStart=/root/.bun/bin/bun run server
Restart=always
RestartSec=3

Environment=NODE_ENV=production
EnvironmentFile=-/etc/portalx/portalx.env

[Install]
WantedBy=multi-user.target
